# Robustness panel 3: adversarial seeding.
# Replace random initial-infected nodes with the top-eigenvector-component
# nodes (worst-case stress test that targets the structural backbone).
seed: 42

sis:
  R0_grid: [1.5, 3.0]
  n_realizations: 30
budget_fractions: [0.0, 0.01, 0.05, 0.10, 0.20]
